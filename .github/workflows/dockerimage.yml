name: speedtest-agent
on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  buildx:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Set up Docker buildx
        id: buildx
        uses: crazy-max/ghaction-docker-buildx@v1
        with:
          buildx-version: latest
          qemu-version: latest
      - name: Available platforms
        run: echo ${{ steps.buildx.outputs.platforms }}
      - uses: actions/docker/login@master
        env:
          DOCKER_REGISTRY_URL: hub.docker.com
          DOCKER_USERNAME: ${{ secrets.DOCKER_HUB_REGISTRY_USERNAME }}
          DOCKER_PASSWORD: ${{ secrets.DOCKER_HUB_REGISTRY_SECRET }}
      - name: Run Buildx
        run: docker buildx build --platform linux/amd64,linux/arm/v7 --file Dockerfile --tag chriskinsman/speedtest-agent:$GITHUB_RUN_NUMBER --output "type=image,push=true" .
        working-directory: speedtest-agent
